# frozen_string_literal: true

RSpec.describe MilleFeuille do
  it "has a version number" do
    expect(MilleFeuille::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
